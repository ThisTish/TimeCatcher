import { getCategory } from "@/server/actions/category/get-categories"

const CategoryContainer = ({ categoryId }: { categoryId: string }) => {

	// const { success, error } = await getCategory(categoryId)
	

	return (
		<>
		</>
	)
}

export default CategoryContainer